import { ApprovalRewardReasionPipe } from './approval-reward-reasion.pipe';

describe('ApprovalRewardReasionPipe', () => {
  it('create an instance', () => {
    const pipe = new ApprovalRewardReasionPipe();
    expect(pipe).toBeTruthy();
  });
});
